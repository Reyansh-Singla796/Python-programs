import time

while True:
    option = int(input("Welcome to the currency converter, here you can convert (usd,aud,bdt,cad,cny,eur,hkd,jpy,kwd and myr) into Indian Rupee, Press 1 to continue and Press 2 to exit (Note!! This Currency converter was made in D-04-M-06-Y-2026, Price may vary): "))

    def usd_con(usd_val):
        ind_val = usd_val * 95.76
        time.sleep(2)
        print(usd_val, "USD =", ind_val, "INR")

    def aud_con(aud_val):
        ind_val = aud_val * 68.25
        time.sleep(2)
        print(aud_val, "AUD =", ind_val, "INR")

    def bdt_con(bdt_val):
        ind_val = bdt_val * 0.78
        time.sleep(2)
        print(bdt_val, "BDT =", ind_val, "INR")

    def cad_con(cad_val):
        ind_val = cad_val * 68.78
        time.sleep(2)
        print(cad_val, "CAD =", ind_val, "INR")

    def cny_con(cny_val):
        ind_val = cny_val * 14.15
        time.sleep(2)
        print(cny_val, "CNY =", ind_val, "INR")

    def eur_con(eur_val):
        ind_val = eur_val * 111.09
        time.sleep(2)
        print(eur_val, "EUR =", ind_val, "INR")

    def hkd_con(hkd_val):
        ind_val = hkd_val * 12.22
        time.sleep(2)
        print(hkd_val, "HKD =", ind_val, "INR")

    def jpy_con(jpy_val):
        ind_val = jpy_val * 0.60
        time.sleep(2)
        print(jpy_val, "JPY =", ind_val, "INR")

    def kwd_con(kwd_val):
        ind_val = kwd_val * 309.96
        time.sleep(2)
        print(kwd_val, "KWD =", ind_val, "INR")

    def myr_con(myr_val):
        ind_val = myr_val * 23.91
        time.sleep(2)
        print(myr_val, "MYR =", ind_val, "INR")

    if option == 2:
        print("Thank you for your kind patience, Bye Bye!!")
        break

    elif option == 1:
        option2 = int(input('''Please select your current currency

1. Convert USD to INR
2. Convert AUD Dollar to INR
3. Convert BDT Taka to INR
4. Convert CAD Dollar to INR
5. Convert CNY Yuan to INR
6. Convert EUR Euro to INR
7. Convert HKD Dollar to INR
8. Convert JPY Yen to INR
9. Convert KWD Dinar to INR
10. Convert MYR Ringgit to INR

Enter Your Input: '''))

        if option2 == 1:
            usd_con(float(input("Enter your currency: ")))
        elif option2 == 2:
            aud_con(float(input("Enter your currency: ")))
        elif option2 == 3:
            bdt_con(float(input("Enter your currency: ")))
        elif option2 == 4:
            cad_con(float(input("Enter your currency: ")))
        elif option2 == 5:
            cny_con(float(input("Enter your currency: ")))
        elif option2 == 6:
            eur_con(float(input("Enter your currency: ")))
        elif option2 == 7:
            hkd_con(float(input("Enter your currency: ")))
        elif option2 == 8:
            jpy_con(float(input("Enter your currency: ")))
        elif option2 == 9:
            kwd_con(float(input("Enter your currency: ")))
        elif option2 == 10:
            myr_con(float(input("Enter your currency: ")))
        else:
            print("loading.")
            time.sleep(1)
            print("loading..")
            time.sleep(1)
            print("loading...")
            time.sleep(1)
            print("Invalid Option!!")

    else:
        print("loading.")
        time.sleep(1)
        print("loading..")
        time.sleep(1)
        print("loading...")
        time.sleep(1)
        print("Invalid Option!!")
